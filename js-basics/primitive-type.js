// Primitives / Value Types과 Reference Types아 있음. 
// Primitives : string, number, boolean, undefined, null => undefined 또한 타입이다.
let name = 'Jinha Jeong'; // string
let lastName = "Jeong";
let age = 30; // number(integer)
let rate = 1.3; // number(float)
let isApproved = false;
let firstName = undefined; // undefined : typeof firstName 해보면 역시 undefined가 나온다. 
let selectedColor = null; // variable의 value를 clear하고 싶을 때 사용한다. 
// => 얘는 typeOf 하면 object가 나온다. 